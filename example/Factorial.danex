(Int result) factorial(n) {
    var acc = 1;
    for (var i = 2; i <= n; i = i + 1;) {
        acc = acc * i;
    }
    result = acc;
}

#- This is a single line comment.

##- This is a
multi-line comment.
-##
